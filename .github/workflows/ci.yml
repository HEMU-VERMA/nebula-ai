name: CI

on:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
      - name: Ruff
        run: ruff check .
      - name: Mypy
        run: mypy src
      - name: Tests
        run: pytest
      - name: Build
        if: matrix.python-version == '3.12'
        run: python -m build
      - name: Verify wheel
        if: matrix.python-version == '3.12'
        run: |
          python -m venv /tmp/nebula-wheel-test
          /tmp/nebula-wheel-test/bin/python -m pip install dist/*.whl
          /tmp/nebula-wheel-test/bin/python -c "import nebula; print(nebula.__version__)"
          /tmp/nebula-wheel-test/bin/python -m pip check
